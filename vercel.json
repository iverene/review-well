{
    "services": {
        "frontend": {
            "root": "frontend",
            "framework": "vite"
        },
        "backend": {
            "root": "backend",
            "entrypoint": "server.js",
            "installCommand": "npm ci --no-audit --no-fund",
            "buildCommand": "node --version && npm --version && pwd && node -p \"require('./node_modules/@prisma/client/package.json').version\" && node -e \"console.log(require.resolve('@prisma/client/package.json'));console.log(require('fs').realpathSync('./node_modules/@prisma/client'))\" && npx prisma generate"
        }
    },
    "rewrites": [
        {
            "source": "/api(/.*)?",
            "destination": {
                "type": "service",
                "service": "backend"
            }
        },
        {
            "source": "/(.*)",
            "destination": {
                "type": "service",
                "service": "frontend"
            }
        }
    ]
}